#
# Hedgin Tactics System
#

menuconfig HTS
	tristate "HTS driver support"
	depends on ARCH_EXYNOS
	default n
	help
	 Enable HTS driver support

if HTS

config HTS_EXT
	bool "HTS Links with External Devices"
	default n
	help
	 Enable HTS Links with External Devices

endif
